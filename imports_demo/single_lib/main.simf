pub use temp::utils::get_two;
use temp::funcs::{get_five, Smth};

fn seven() -> u32 {
    7
}

fn main() {
    let (_, temp): (bool, u32) = jet::add_32(get_two(), get_five());
    assert!(jet::eq_32(temp, seven()));
}